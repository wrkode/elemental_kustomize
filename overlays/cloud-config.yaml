apiVersion: elemental.cattle.io/v1beta1
kind: SeedImage
metadata:
  name: elemental-os-seedimage-v5
  namespace: fleet-default
spec:
  cloud-config:
    write_files:
        path: /opt
        permissions: 0777
        content:
        - |
          name: "Create LVM logic volumes"
          
              